name: Amy SMC Y Round 5 Final

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/amy-smc-y-round5-final.yml'

permissions:
  contents: write

jobs:
  round5-final:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Apply 16-year conditional Round 5 best-of candidate
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          x_before="$(git hash-object Amy-SMC-X.pine)"
          original_before="$(git hash-object Amy-SMC.pine)"
          python .github/scripts/amy_smc_y_round5_final.py
          test "$(git hash-object Amy-SMC-X.pine)" = "$x_before"
          test "$(git hash-object Amy-SMC.pine)" = "$original_before"
          git add Amy-SMC-Y.pine
          git diff --cached --check
          changed="$(git diff --cached --name-only | tr -d '\r')"
          printf '%s\n' "$changed"
          test "$changed" = 'Amy-SMC-Y.pine'
          git commit -m 'Refine Amy SMC Y predictive round 5 with 16-year regimes'
          git push
